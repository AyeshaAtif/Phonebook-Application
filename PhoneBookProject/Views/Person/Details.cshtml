@model IEnumerable<PhoneBookProject.Models.ContactViewModel>

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<p>
@foreach (var item in Model)
{
    @Html.ActionLink("Create New", "Create", "Contact", new { id = item.PersonId }, null)
}

</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ContactId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContactNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PersonId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ContactId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContactNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PersonId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", "Contact", new { id = item.PersonId }, null) |
           
            @Html.ActionLink("Delete", "Delete", "Contact", new {id = item.ContactId }, null)
        </td>
    </tr>
}

</table>
